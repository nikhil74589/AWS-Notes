Bare Metal means khud ka apna 

sudo apt update
sudo apt install mysql-server -y
mysql --version
sudo systemctl start mysql
sudo systemctl enable mysql
sudo systemctl status mysql

sudo mysql -u root -p
sudo mysql

SELECT 1;
SELECT VERSION();
SELECT USER ();
ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY
FLUSH PRIVILEGES;

sudo mysql -u root -p
paste password

SHOW DATABASES;
CREATE DATABASE DB_NAME;
USE DB_NAME;

CREATE TABLE Devops (stu_name varchar(100), stu_add varchar(100) );
show tables;
desc Devops;

INSERT INTO INSERT INTO Devops values ('Nikhil','Gorakhpur');
SELECT * FROM Devops;

REMOTE-USER
CREATE USER 'root'@'%' IDENTIFIED WITH mysql_native_password BY 'Admin@123';

GRANT ALL PRIVILEGES ON *.* TO 'root'@'%';
FLUSH PRIVILEGES;

sudo vim /etc/mysql/mysql.conf.d/mysqld.cnf
